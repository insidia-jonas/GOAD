---
# Attacker VMs Playbook
# Installs tools on Kali and Commando VMs

# Commando VM Installation (Windows)
- name: Install Commando VM
  hosts: commando
  gather_facts: true
  vars:
    hosts_dict: "{{ lab.hosts | default({}) }}"
  tasks:
    - name: Debug - Show connection info
      debug:
        msg: "Connected to {{ inventory_hostname }} at {{ ansible_host }}"

    - name: Disable Windows Defender Real-Time Protection
      ansible.windows.win_powershell:
        script: |
          Set-MpPreference -DisableRealtimeMonitoring $true -ErrorAction SilentlyContinue
          Set-MpPreference -DisableBehaviorMonitoring $true -ErrorAction SilentlyContinue
          Set-MpPreference -DisableBlockAtFirstSeen $true -ErrorAction SilentlyContinue
          Set-MpPreference -DisableIOAVProtection $true -ErrorAction SilentlyContinue
          Set-MpPreference -DisablePrivacyMode $true -ErrorAction SilentlyContinue
          Set-MpPreference -SignatureDisableUpdateOnStartupWithoutEngine $true -ErrorAction SilentlyContinue
          Set-MpPreference -DisableArchiveScanning $true -ErrorAction SilentlyContinue
          Set-MpPreference -DisableIntrusionPreventionSystem $true -ErrorAction SilentlyContinue
          Set-MpPreference -DisableScriptScanning $true -ErrorAction SilentlyContinue
          Set-MpPreference -SubmitSamplesConsent 2 -ErrorAction SilentlyContinue
      ignore_errors: yes

    - name: Disable Windows Defender via Registry
      ansible.windows.win_regedit:
        path: HKLM:\SOFTWARE\Policies\Microsoft\Windows Defender
        name: DisableAntiSpyware
        data: 1
        type: dword
      ignore_errors: yes

    - name: Disable Windows Defender Real-Time Protection via Registry
      ansible.windows.win_regedit:
        path: HKLM:\SOFTWARE\Policies\Microsoft\Windows Defender\Real-Time Protection
        name: DisableRealtimeMonitoring
        data: 1
        type: dword
      ignore_errors: yes

    - name: Set PowerShell Execution Policy to Unrestricted
      ansible.windows.win_powershell:
        script: |
          Set-ExecutionPolicy Unrestricted -Force -Scope LocalMachine

    - name: Create Commando VM directory
      ansible.windows.win_file:
        path: C:\commando-vm
        state: directory

    - name: Check if Commando VM is already downloaded
      ansible.windows.win_stat:
        path: C:\commando-vm\install.ps1
      register: commando_installer

    - name: Download Commando VM from GitHub
      ansible.windows.win_powershell:
        script: |
          [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
          $ProgressPreference = 'SilentlyContinue'

          # Download latest release
          $zipUrl = "https://github.com/mandiant/commando-vm/archive/refs/heads/main.zip"
          $zipPath = "C:\commando-vm\commando-vm.zip"

          Write-Host "Downloading Commando VM from $zipUrl..."
          Invoke-WebRequest -Uri $zipUrl -OutFile $zipPath -UseBasicParsing

          # Extract
          Write-Host "Extracting..."
          Expand-Archive -Path $zipPath -DestinationPath "C:\commando-vm" -Force

          # Move files to root
          Get-ChildItem "C:\commando-vm\commando-vm-main\*" | Move-Item -Destination "C:\commando-vm\" -Force

          # Cleanup
          Remove-Item "C:\commando-vm\commando-vm-main" -Recurse -Force -ErrorAction SilentlyContinue
          Remove-Item $zipPath -Force -ErrorAction SilentlyContinue

          # Unblock files
          Get-ChildItem "C:\commando-vm" -Recurse | Unblock-File

          Write-Host "Commando VM downloaded successfully"
      when: not commando_installer.stat.exists
      register: download_result

    - name: Show download result
      debug:
        var: download_result
      when: download_result is defined

    - name: Check if Commando VM installation is complete
      ansible.windows.win_stat:
        path: C:\commando-vm\.installed
      register: commando_installed

    - name: Create install script wrapper
      ansible.windows.win_copy:
        content: |
          Set-Location C:\commando-vm
          .\install.ps1 -cli -nochecks
          if ($LASTEXITCODE -eq 0) {
              New-Item -Path "C:\commando-vm\.installed" -ItemType File -Force
          }
        dest: C:\commando-vm\run_install.ps1
      when: not commando_installed.stat.exists

    - name: Display installation instructions
      debug:
        msg: |
          Commando VM is downloaded to C:\commando-vm

          To install manually (takes 2-4 hours), run on the Commando VM:
            1. Open PowerShell as Administrator
            2. cd C:\commando-vm
            3. .\install.ps1 -cli -nochecks

          Or run: C:\commando-vm\run_install.ps1
      when: not commando_installed.stat.exists

# Kali Linux - already comes with tools, just ensure it's ready
- name: Configure Kali Linux
  hosts: kali
  gather_facts: true
  become: true
  tasks:
    - name: Debug - Show connection info
      debug:
        msg: "Connected to {{ inventory_hostname }} at {{ ansible_host }}"

    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600
      ignore_errors: yes

    - name: Ensure common pentesting tools are installed
      apt:
        name:
          - nmap
          - netcat-openbsd
          - gobuster
          - crackmapexec
          - impacket-scripts
          - bloodhound
          - neo4j
          - evil-winrm
          - responder
          - john
          - hashcat
          - smbclient
          - ldap-utils
          - python3-pip
          - pipx
        state: present
      ignore_errors: yes

    - name: Install certipy via pipx
      become: true
      become_user: kali
      shell: pipx install certipy-ad
      ignore_errors: yes

    - name: Install coercer via pipx
      become: true
      become_user: kali
      shell: pipx install coercer
      ignore_errors: yes

    - name: Display Kali setup status
      debug:
        msg: "Kali Linux is ready for pentesting"
